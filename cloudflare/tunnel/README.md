# Cloudflare Tunnel — Sistema OG

Publica o app local (porta 4321) na internet. O PC precisa permanecer ligado com `npm run og:start`.

Quick test:
```
cloudflared tunnel --url http://127.0.0.1:4321
```

Ver também `CLOUDFLARE-TUNNEL.txt` na raiz.
